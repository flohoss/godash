version: v2
auth_server: tp.unjx.de:443
onboarding:
  join_method: gitlab
  token: gitlab
oneshot: true
storage:
  type: memory
outputs:
  - type: identity
    destination:
      type: directory
      path: /tmp/machine-id
